spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        #        show_sql: true
#        format_sql: true         - Spring Boot에서는 별도의 설정이 없으면, Memory 모드로 컴파일.

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace